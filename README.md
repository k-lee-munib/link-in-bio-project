# link-in-bio-project

A fully responsive "Link-in-Bio" website powered by a Python backend.
Unlike static HTML sites, this project uses **Flask** to dynamically inject data (User Name, Skills, Links) into the frontend.

## 🛠️ Tech Stack
* **Backend:** Python 3, Flask
* **Frontend:** HTML5, CSS3, JavaScript
* **Template Engine:** Jinja2
* **Automation:** Python lists serve data to the HTML

## 🌟 Features
* **Dynamic Rendering:** Skills and names are managed in Python, not hardcoded in HTML.
* **Dark Mode Toggle:** JavaScript DOM manipulation to switch themes.
* **Responsive Design:** CSS styling for mobile and desktop views.

## 📸 Usage
1.  Clone the repository.
2.  Run `pip install flask`.
3.  Run `python app.py`.
4.  Visit `localhost:5000` to see the magic!
